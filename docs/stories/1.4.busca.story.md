# Story 1.4: Busca

## Status

Ready

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["pnpm lint", "pnpm typecheck", "pnpm test", "teste de contrato do storesearch com fixture", "navegação por teclado nos dois idiomas"]
```

## Story

**Como** visitante,
**quero** buscar um jogo pelo nome,
**para que** eu chegue à página dele.

## Acceptance Criteria

1. Campo de busca no cabeçalho e na home.
2. Resultados com capa e nome, vindos do `storesearch` da loja Steam, e só jogos com conquistas.
3. Busca por AppID numérico abre o jogo direto.
4. A resposta da busca já traz o campo `guided` (sempre falso até existir guia); a ordem e
   o selo dos jogos com guia entram na Story 2.4.
5. Se a busca da Steam falhar, a página diz isso e oferece a busca por AppID.

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` in core-config.yaml

## Tasks / Subtasks

- [ ] **Task 1: Serviço de busca** (AC: 2, 4, 5)
  - [ ] 1.1 Em `src/server/steam/catalog.ts`, função `search(term, locale)` que chama `store.steampowered.com/api/storesearch/?term={termo}&l={l}&cc={cc}` pelo cliente de `src/server/steam/http.ts` (sem chave), com timeout de 8 s, retry e contagem em `steam_api_usage` da Story 1.2. Idioma e país por locale: `pt` → `l=brazilian&cc=BR`; `en` → `l=english&cc=US` (D1).
  - [ ] 1.2 zod da resposta do `storesearch` em `src/server/steam/responses.ts`: `{ total, items: [{ type, name, id, tiny_image }] }`, com os demais campos ignorados. Itens com `type` diferente de `"app"` são descartados. Formato inválido lança `SteamUnavailableError` e registra no log `code: 'STEAM_CONTRACT'` (risco R1).
  - [ ] 1.3 Filtro "só jogos com conquistas": para cada item, se já está em `steam_game`, usa o `status` (`ok` entra; `no_achievements`/`not_found` dentro de `next_retry_at` sai); senão, sonda com `GetGlobalAchievementPercentagesForApp` (sem chave) e grava a linha em `steam_game` com `status = 'ok'` ou `no_achievements` (cache negativo de 7 dias), sem gravar conquistas nem `percentages_fetched_at`. Sondagens em paralelo, no máximo 5 ao mesmo tempo. Sondagem que falha (timeout, 5xx) tira o item da lista sem gravar nada e marca a resposta como parcial (D4).
  - [ ] 1.4 Guardar o nome vindo da busca em `steam_game` (`name_pt` na busca em `pt`, `name_en` na busca em `en`), só quando o campo ainda é nulo. A capa não vem da busca (D5).
  - [ ] 1.5 Cache do mesmo termo e idioma por 24 h no Data Cache do Next (`fetch(..., { next: { revalidate: 86400 } })`, repassado pelo `http.ts`). O termo é normalizado (espaços nas pontas e repetidos, minúsculas) antes de montar a URL, para o cache acertar mais.
  - [ ] 1.6 Orçamento (`src/server/budget.ts`): acima de 100%, não sonda apps novos; devolve só os itens já conhecidos com `status = 'ok'` e marca a resposta como parcial. Nas faixas abaixo de 100%, sonda normalmente (é ação do usuário, como a primeira visita) (D4).
  - [ ] 1.7 Campo `guided` em cada item a partir de `steam_game.is_guided` (sempre falso até a Story 2.1/2.5).
  - [ ] 1.8 Ordem da resposta = ordem de relevância do `storesearch` (a Story 2.4 põe os com guia primeiro).

- [ ] **Task 2: Route Handler** (AC: 2, 4, 5)
  - [ ] 2.1 `src/app/api/search/route.ts`: `GET /api/search?q=&locale=`, sem auth.
  - [ ] 2.2 Validação zod: `q` com 1 a 80 caracteres depois do `trim`, `locale` em `pt`/`en`.
  - [ ] 2.3 Resposta 200 `{ items: [{ appid, name, image, guided }], partial: boolean, capped: boolean }`. `name` = `name_pt`/`name_en` do banco pelo idioma ou o nome da busca; `image` = URL derivada da capa 460 x 215 (D5); `capped` = a Steam devolveu o máximo de 10 itens (D3). `partial` e `capped` são acréscimos ao contrato da arquitetura 8.2.
  - [ ] 2.4 Falha da Steam: 503 com `{ error: { code: 'STEAM_UNAVAILABLE', message } }`; entrada inválida: 400 `VALIDATION`.
  - [ ] 2.5 A rota não chama a Steam direto; só o serviço de `src/server/steam/`. Contador de chamadas gravado com `after()` (padrão da 1.2).

- [ ] **Task 3: Página de busca** (AC: 2, 3, 5)
  - [ ] 3.1 `src/app/[locale]/busca/page.tsx`, publicada como `/pt/busca?q=` e `/en/search?q=` pelo `pathnames` que já existe em `src/i18n/routing.ts` (o `?q=` não é traduzido). Casca estática (`setRequestLocale`), com um Client Component de resultados que lê `?q=` por `useSearchParams` dentro de `<Suspense>` (exigência do Next para página estática) e chama `/api/search`. H1 "Buscar jogos" / "Search games".
  - [ ] 3.2 Resultados com o cartão de jogo (front-end spec 5.12): capa 460 x 215 (proporção 2,14:1, `aspect-ratio` reservado) com `<img loading="lazy" decoding="async">` direto do CDN, nome em até 2 linhas com reticências, AppID em fonte mono (`font-mono`). Jogos sem guia não mostram nota nem horas. Estados: padrão, hover (borda `accent`), foco, carregando (esqueleto). Capa que não carrega (`onError`): some a imagem e fica o bloco reservado em `surface-2`. Grade de 1 coluna no celular, 2 no tablet e 3 no desktop.
  - [ ] 3.3 Cada resultado é um `Link` do next-intl para `{ pathname: '/jogo/[appid]', params: { appid } }`.
  - [ ] 3.4 Falha da Steam (503): mensagem dizendo que a busca da Steam falhou e o próprio campo de busca da página recebe o foco para digitar o AppID (AC 5). Texto em D6.
  - [ ] 3.5 Contagem de resultados anunciada numa região `aria-live="polite"`.
  - [ ] 3.6 Estado vazio (nenhum item depois do filtro): texto de D6 com a dica do AppID. `partial`: nota de D6 acima da lista. `capped`: dica de D6 abaixo da lista. `q` vazio: só o campo, sem chamada.

- [ ] **Task 4: Busca por AppID** (AC: 3)
  - [ ] 4.1 Termo só com dígitos (`/^\d{1,10}$/` depois do `trim`, e valor até 2.147.483.647) abre direto a página do jogo pelo `useRouter` do next-intl (`/pt/jogo/{appid}` ou `/en/game/{appid}`), sem chamar o `storesearch`. Vale no envio do `SearchBox` e também ao abrir `/pt/busca?q=367520` direto (`router.replace`).
  - [ ] 4.2 AppID inexistente ou sem conquistas cai no `not-found.tsx` do jogo (Story 1.3); acrescentar nele o `SearchBox` (D2 da 1.3).

- [ ] **Task 5: Campo de busca no cabeçalho e na home** (AC: 1)
  - [ ] 5.1 `SearchBox` (Client Component): `<form role="search">` com `<input type="search" name="q">`, rótulo acessível e placeholder "Buscar jogo ou AppID..." (texto via mensagens). Sem sugestões enquanto digita nesta story (D2).
  - [ ] 5.2 No `SiteHeader`, no centro (entre a marca e o interruptor de spoilers da 1.3). No celular (abaixo do breakpoint `md`), ícone de lupa com `aria-label` que abre a busca em tela cheia (`role="dialog"` `aria-modal="true"`, foco no campo); Esc e o botão "Fechar" fecham e devolvem o foco à lupa.
  - [ ] 5.3 Na home da 1.1 (`src/app/[locale]/page.tsx`), campo "Buscar qualquer jogo da Steam" com botão "Buscar" (front-end spec 4.3), abaixo do texto de apresentação.
  - [ ] 5.4 Enviar leva para `/pt/busca?q=` ou `/en/search?q=` (`router.push` com `{ pathname: '/busca', query: { q } }`), ou direto ao jogo se for AppID (Task 4).
  - [ ] 5.5 Rótulo acessível, alvo de toque de 44 px, foco visível (o `:focus-visible` global da 1.1 já cobre).

- [ ] **Task 6: Mensagens** (AC: 1, 5)
  - [ ] 6.1 Textos novos em `messages/pt-BR.json` e `messages/en.json` com as mesmas chaves (tabela D6); sem travessão (o teste da 1.1 confere).

- [ ] **Task 7: Testes** (AC: 2, 3, 4, 5)
  - [ ] 7.1 Fixtures em `fixtures/steam/storesearch/`: `hollow-knight.brazilian.json` e `hollow-knight.english.json` gravadas com o `pnpm steam:snapshot` da 1.2 ou com `curl` (endpoint sem chave; a busca "hollow knight" traz o jogo, a sequência, a trilha sonora e a DLC); `vazio.json` (`{"total":0,"items":[]}`); `formato-mudado.json` (escrita à mão). Mais as fixtures de % da trilha sonora `598190` (403) já gravadas na 1.2.
  - [ ] 7.2 Teste de contrato: o zod aceita as fixtures gravadas e acusa a de formato mudado (risco R1).
  - [ ] 7.3 Integração da rota: só jogos com conquistas voltam (trilha sonora e DLC saem); `guided` presente e falso; app já em `steam_game` não é sondado de novo (zero chamadas de %); Steam fora devolve 503 `STEAM_UNAVAILABLE`; `q` com mais de 80 caracteres devolve 400 `VALIDATION`; orçamento acima de 100% devolve só os conhecidos com `partial: true` e zero sondagens.
  - [ ] 7.4 Unitários: detecção de AppID numérico (limites: `0`, `2147483647`, `2147483648`, texto com dígitos e letras) e normalização do termo.
  - [ ] 7.5 Manual: navegação por teclado da busca (cabeçalho, home, página de resultados, tela cheia no celular com Esc) nos dois idiomas.

## Dev Notes

### Stories anteriores

- 1.1 (código em `src/`): `SiteHeader` em `src/components/site-header.tsx` (marca à esquerda,
  `LocaleSwitcher` à direita), home provisória em `src/app/[locale]/page.tsx`, `pathnames` com
  `/busca` → `/en/search` em `src/i18n/routing.ts`, `Link`/`useRouter` em
  `src/i18n/navigation.ts`, tokens em `src/app/globals.css`, CSP com `img-src
  https://*.steamstatic.com` (cobre as capas de `shared.akamai.steamstatic.com`) e `connect-src
  'self'` (o cliente só chama `/api/search`).
- 1.2: `src/server/steam/http.ts` (timeout, retry, zod, `fetchImpl`, `init` extra, contagem em
  `steam_api_usage`), `errors.ts`, `budget.ts` (soma só a Web API; `storesearch` fica fora,
  D4 da 1.2), `steam_game` com `status` e cache negativo de 7 dias, `pnpm steam:snapshot`.
- 1.3: página do jogo, `jogo/[appid]/not-found.tsx`, `SpoilerToggle` no cabeçalho.

### Arquivos desta story

```text
src/app/[locale]/busca/page.tsx
src/app/[locale]/page.tsx              # alterado: campo da home
src/app/[locale]/jogo/[appid]/not-found.tsx   # alterado: SearchBox
src/app/api/search/route.ts
src/server/steam/catalog.ts            # search
src/server/steam/responses.ts          # zod do storesearch
src/components/                        # SearchBox, SearchResults, GameCard
src/components/site-header.tsx         # alterado: SearchBox no centro
src/domain/search-term.ts              # detecção de AppID e normalização do termo
fixtures/steam/storesearch/…
messages/pt-BR.json, messages/en.json
tests/unit/…, tests/integration/…
```

[Source: docs/fullstack-architecture.md#4 Estrutura do código]

### Por que `storesearch`

`ISteamApps/GetAppList` está obsoleto e nem ele nem `IStoreService/GetAppList` são usados no
MVP: indexar o catálogo custaria armazenamento e não diria quais apps têm conquistas. O
`storesearch` (não documentado, sem chave, usado pela própria loja, testado em 25/09/2026) traz
relevância, nome localizado e capa. Plano B se sair do ar: índice local por
`IStoreService/GetAppList` com `pg_trgm` (fora desta story).
[Source: docs/fullstack-architecture.md#9.1 Endpoints usados] [Source: docs/fullstack-architecture.md#18 Riscos (R1)]

Formato conferido pelo @po em 25/09/2026 (`term=hollow knight`): `{"total":5,"items":[{"type":"app","name":"Hollow Knight","id":367520,"price":{...},"tiny_image":"https://shared.akamai.steamstatic.com/store_item_assets/steam/apps/367520/<hash>/capsule_231x87.jpg?t=...","metascore":"87","platforms":{...},...}]}`.
No máximo 10 itens, sem paginação; termo sem resultado devolve `{"total":0,"items":[]}`;
trilha sonora (`598190`) e DLC (`916000`) vêm como `type: "app"`.

### Sondagem de conquistas

Trilhas sonoras e DLCs vêm como `type: "app"`. Cada resultado passa pela sondagem: status do
banco, ou `GetGlobalAchievementPercentagesForApp` (sem chave; app sem conquistas responde `{}`
com 403) e grava o status. Resultados do mesmo termo no Data Cache por 24 h.
[Source: docs/fullstack-architecture.md#9.3 Catálogo]

### Rotas

- `/[locale]/busca`: casca estática, busca no cliente via `/api/search`, cache de dados de 24 h.
  [Source: docs/fullstack-architecture.md#8.1]
- `GET /api/search?q=&locale=`: sem auth, 200 `{ items: [{appid, name, image, guided}] }`
  (mais `partial` e `capped`, D3/D4). Erros no formato único com `code`. [Source: docs/fullstack-architecture.md#8.2 Route Handlers]
- Rotas públicas: `/pt/busca?q={termo}` e `/en/search?q={termo}`. [Source: docs/front-end-spec.md#2.1]

### Segurança

Termo de busca até 80 caracteres, validado por zod e codificado com `encodeURIComponent` na URL
da Steam. A regra da WAF (Story 1.1) cobre `^/api/(search|auth|me)` com 60 requisições por 60 s
por IP. Nomes vindos da Steam sempre como texto. [Source: docs/fullstack-architecture.md#13.1] [Source: docs/fullstack-architecture.md#13.3]

### Componentes

- `SearchBox` é Client Component. [Source: docs/fullstack-architecture.md#12.2]
- Cabeçalho: busca no centro; no celular, lupa com tela cheia. [Source: docs/front-end-spec.md#2.2 Navegação]
- Cartão de jogo (5.12). [Source: docs/front-end-spec.md#5.12]
- Esc fecha a busca em tela cheia devolvendo o foco. [Source: docs/front-end-spec.md#7.2]
- Capas sem `next/image`. [Source: docs/fullstack-architecture.md#12.6]

### Decisões do @po (resolvem as lacunas G1 a G6 do @sm)

- **D1 (G1). `l` e `cc` por idioma:** `pt` → `l=brazilian&cc=BR`, `en` → `l=english&cc=US`.
  Mesmos códigos de idioma da Web API (arquitetura 9.1); o país define moeda e disponibilidade
  regional, e o público do `pt` é o Brasil. Conferido: as duas combinações respondem.
- **D2 (G2). Sem sugestões enquanto digita** nesta story: o campo envia para a página de
  resultados. O "combobox com sugestões" do front-end spec 5.16 fica fora do MVP (cada letra
  custaria uma busca e até 10 sondagens). Ver "Decisões para o Lucas".
- **D3 (G3). Quantidade e vazio:** mostra todos os itens que passam no filtro (a Steam devolve
  no máximo 10), sem paginação. Com 10 itens brutos, `capped: true` e dica para refinar o termo.
- **D4 (G4). Orçamento na sondagem:** abaixo de 100%, sonda normalmente; acima, só os já
  conhecidos, com `partial: true` e nota na tela. Sondagem que falha também deixa `partial`.
- **D5 (G5). Capa:** o `storesearch` só traz `tiny_image` (231 x 87), menor que o cartão
  (460 x 215). A resposta usa a capa derivada
  `https://shared.akamai.steamstatic.com/store_item_assets/steam/apps/{appid}/header.jpg`
  (conferida: 200), ou `steam_game.header_image_url` quando já existe (o `appdetails` da 1.2
  prevalece). A busca não grava capa no banco.
- **D6. Textos novos:**

  | Chave | PT | EN |
  |---|---|---|
  | `Search.title` | Buscar jogos | Search games |
  | `Search.placeholder` | Buscar jogo ou AppID... | Search game or AppID... |
  | `Search.label` | Buscar jogo pelo nome ou AppID | Search for a game by name or AppID |
  | `Search.homeLabel` | Buscar qualquer jogo da Steam | Search any Steam game |
  | `Search.submit` | Buscar | Search |
  | `Search.open` / `Search.close` | Abrir busca / Fechar busca | Open search / Close search |
  | `Search.count` | {count, plural, =0 {Nenhum resultado} one {# resultado} other {# resultados}} | {count, plural, =0 {No results} one {# result} other {# results}} |
  | `Search.empty` | Nenhum jogo com conquistas encontrado para "{term}". | No games with achievements found for "{term}". |
  | `Search.emptyHint` | Confira a grafia ou busque pelo AppID, o número que aparece no endereço da página do jogo na Steam. | Check the spelling or search by AppID, the number in the game's Steam store URL. |
  | `Search.failed` | A busca da Steam não respondeu agora. Se você sabe o AppID do jogo, digite o número no campo acima. | Steam search isn't responding right now. If you know the game's AppID, type the number in the field above. |
  | `Search.partial` | Alguns resultados podem estar faltando porque a Steam não respondeu a tempo ou o limite diário de consultas foi atingido. Tente de novo mais tarde. | Some results may be missing because Steam didn't respond in time or the daily lookup limit was reached. Please try again later. |
  | `Search.capped` | A Steam mostra no máximo 10 resultados. Não achou? Use um termo mais específico ou o AppID. | Steam shows at most 10 results. Can't find it? Try a more specific term or the AppID. |

- **G6** sem mudança: ordem e selo "Guia" são da Story 2.4.

### Testing

- Vitest em `tests/unit/` e `tests/integration/` com o Postgres de teste da 1.2
  (`TEST_DATABASE_URL`); fixtures em `fixtures/steam/storesearch/`. Nenhum teste chama a Steam real.
  [Source: docs/fullstack-architecture.md#14.1 Pirâmide] [Source: docs/fullstack-architecture.md#4]
- Teste de contrato com fixture detecta mudança de formato do `storesearch`. [Source: docs/fullstack-architecture.md#18 (R1)]
- Tela nova: navegação por teclado nos dois idiomas. [Source: docs/prd.md#4 Testes]

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-09-25 | 1.0 | Criação a partir do PRD 1.2, arquitetura 1.1 e front-end spec 1.1 | @sm (River) |
| 2026-09-25 | 1.1 | Validada GO (8/10): lacunas G1 a G5 resolvidas (idioma e país, sem autocompletar, limite de 10, orçamento na sondagem, capa derivada), textos PT/EN, estado vazio e parcial; Status: Draft → Ready | @po (Pax) |

## Dev Agent Record

### Agent Model Used

_A preencher pelo @dev._

### Debug Log References

_A preencher pelo @dev._

### Completion Notes List

_A preencher pelo @dev._

### File List

_A preencher pelo @dev._

## QA Results

_A preencher pelo @qa._

## PO Validation

**Data:** 2026-09-25 · **Validador:** @po (Pax) · **Tarefa:** `validate-next-story` ·
**Resultado:** GO · **Nota:** 8/10 · **Confiança:** alta

| # | Item do checklist | Resultado | Observação |
|---|---|---|---|
| 1 | Template completo | Passa | Todas as seções presentes, sem placeholder. |
| 1.1 | Executor e quality gate | Passa | `@dev` / `@architect`, diferentes. |
| 2 | Arquivos e estrutura | Passa após ajuste | Arquivos alterados da 1.1 e 1.3 listados; `search-term.ts` para a lógica pura. |
| 3 | UI | Passa após ajuste | Estados vazio, parcial, limite e falha definidos; tela cheia no celular com `dialog`; capa com reserva de espaço. |
| 4 | ACs cobertos pelas tasks | Passa | AC1 a AC5 mapeados. |
| 5 | Testes | Passa após ajuste | Fixtures reais com origem definida; orçamento e sondagem cobertos. |
| 6 | Segurança | Passa | zod, 80 caracteres, `encodeURIComponent`, WAF, texto escapado. |
| 7 | Sequência das tasks | Passa | Depende da 1.2 (cliente, orçamento) e da 1.3 (`not-found` do jogo, cabeçalho). |
| 8 | CodeRabbit | N/A | Desabilitado; aviso presente. |
| 9 | Anti-alucinação | Passa após ajuste | Formato do `storesearch` conferido na API pública: só `tiny_image` 231 x 87 (o cartão pede 460 x 215) e máximo de 10 itens. |
| 10 | Pronta para o dev | Passa | Lacunas G1 a G6 resolvidas (D1 a D6). |

**Achados corrigidos:** `l`/`cc` sem valor; autocompletar indefinido; sem limite nem estado
vazio; sondagem sem regra de orçamento; capa da busca menor que a do cartão; `useSearchParams`
sem `<Suspense>` quebraria a página estática; sondagem sem limite de paralelismo nem regra de
falha; textos sem inglês.

**Nota não é 9 porque:** a sondagem torna o tempo de resposta da primeira busca de um termo
dependente de até 10 chamadas à Steam (em paralelo, 5 por vez). É aceitável, mas vale medir no
preview.

**Decisões para o Lucas** (nenhuma bloqueia):

1. **Autocompletar no campo de busca** (front-end spec 5.16 fala em "sugestões"). Proposta do
   @po: fica fora do MVP, só envio para a página de resultados (D2). Se quiser sugestões, vira
   story nova depois do lançamento.
2. **País da loja no inglês** (`cc=US`). Afeta só moeda e jogos bloqueados por região, que a
   busca não mostra. Se preferir outro país, é uma troca de constante.
