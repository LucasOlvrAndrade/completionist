# Story 1.3: Página do jogo sem guia

## Status

Draft

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@ux-design-expert"
quality_gate_tools: ["pnpm lint", "pnpm typecheck", "pnpm test", "navegação por teclado nos dois idiomas", "conferência do spoiler fora do DOM e da árvore de acessibilidade"]
```

## Story

**Como** visitante,
**quero** abrir qualquer jogo e ver as conquistas da mais fácil para a mais difícil,
**para que** eu saiba por onde começar.

## Acceptance Criteria

1. `/pt/jogo/{appid}` e `/en/game/{appid}` (pasta interna `app/[locale]/jogo/[appid]`, FR26)
   mostram capa, nome e a lista das conquistas com ícone, nome, descrição, % global e o selo
   de raridade nas cinco faixas do FR2.
2. Ordem pela % decrescente, com teste unitário da ordenação (% nula no fim, desempate pela
   ordem do schema).
3. Aviso "O guia deste jogo ainda não foi produzido" (FR3).
4. Conquistas ocultas embaçadas com clique para revelar (FR5). O texto oculto não fica no
   DOM visível nem na árvore de acessibilidade até ser revelado; aviso `<noscript>` para
   quem navega sem JavaScript.
5. Interruptor global de spoilers no cabeçalho, salvo no navegador (FR6, parte visitante).
6. Nomes e descrições no idioma do site, com inglês como alternativa (FR27).
7. A página registra a visita (`POST /api/visit/{appid}`, no máximo uma escrita por hora por
   jogo), que alimenta a fila de "visitados nos últimos 30 dias" do cron (Story 1.2).

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` in core-config.yaml

## Tasks / Subtasks

- [ ] **Task 1: Funções de domínio** (AC: 1, 2, 6)
  - [ ] 1.1 `src/domain/rarity.ts`: função única das faixas, fechadas embaixo e abertas em cima (Comum `p ≥ 50`, Incomum `20 ≤ p < 50`, Rara `5 ≤ p < 20`, Muito rara `1 ≤ p < 5`, Ultra rara `p < 1`, nulo = "sem dado"), comparando com 3 casas decimais. A mesma função vai servir ao `d_eff` da Story 2.2.
  - [ ] 1.2 `src/domain/ordering.ts`, variante sem guia: `globalPercent` decrescente (nulo como `-1`, vai para o fim), depois `displayOrder` crescente; % comparada com 3 casas decimais; conquistas com `removed_at` não aparecem.
  - [ ] 1.3 `src/domain/naming.ts` (parte Steam de 11.4): nome = `steam_text[L].name ?? steam_text['en'].name ?? apiname`; descrição = `steam_text[L].description ?? steam_text['en'].description ?? ''`; oculta sem descrição mostra "Conquista oculta". A camada do guia (`guia.nomes`) fica para a 2.2.
  - [ ] 1.4 Testes de tabela em `tests/unit/` para as três funções (limites exatos 50, 20, 5, 1; % nula; empate pela ordem do schema; fallback de idioma).

- [ ] **Task 2: Rota e renderização da página** (AC: 1, 6)
  - [ ] 2.1 `src/app/[locale]/jogo/[appid]/page.tsx` (Server Component) publicada como `/pt/jogo/{appid}` e `/en/game/{appid}` pelo `pathnames` da 1.1.
  - [ ] 2.2 ISR: `revalidate = 86400`, `dynamicParams = true`; `generateStaticParams` devolve os jogos com guia × 2 idiomas (lista vazia nesta story e com `CI_BUILD=1`).
  - [ ] 2.3 Validar `appid` com `z.coerce.number().int().min(1).max(2_147_483_647)`; fora disso, `notFound()`.
  - [ ] 2.4 Chamar `ensureGame(appid)` da Story 1.2; `no_achievements`/`not_found` → `notFound()` (404 em cache); falha da Steam → lançar erro (o Next não grava a versão com erro); orçamento estourado → erro "tente mais tarde", não 404.
  - [ ] 2.5 Sem `new Date()` nem `Math.random()` no HTML; data do cache vem do banco.
  - [ ] 2.6 `generateMetadata`: título e descrição com o nome do jogo, Open Graph com a capa da Steam, `alternates.languages` com `pt-BR` e `en` apontando para os caminhos traduzidos. Nunca nome de conquista oculta no `<title>` nem no Open Graph.
  - [ ] 2.7 Nota única "Alguns nomes estão em inglês porque o jogo não tem tradução na Steam" quando algum texto `pt-BR` for `is_fallback` (front-end spec 10).

- [ ] **Task 3: Cabeçalho do jogo e aviso sem guia** (AC: 1, 3)
  - [ ] 3.1 Variante sem guia do cabeçalho da nota (front-end spec 5.3): capa (`header_image_url`), nome, total de conquistas.
  - [ ] 3.2 Capa com `<img>` direto do CDN da Steam, `width`/`height` fixos e prioridade de carregamento (elemento de LCP); sem `next/image`.
  - [ ] 3.3 Componente do aviso (front-end spec 5.11, variante sem guia): "O guia deste jogo ainda não foi produzido. As conquistas estão ordenadas pela porcentagem de jogadores que já desbloquearam." Faixa no topo da lista, fundo `surface`, borda esquerda em `info`. Texto via `messages/*.json`.

- [ ] **Task 4: Cartão de conquista e selo de raridade** (AC: 1, 2)
  - [ ] 4.1 `AchievementCard` no estado "Sem guia" (front-end spec 5.1): ícone 64 px (48 px no celular), nome (H3), descrição, selo de raridade; não abre, sem seta.
  - [ ] 4.2 Ícones com `<img loading="lazy" decoding="async" width height>`, sem `next/image`; `alt` vazio quando o nome está ao lado; ícone de oculta embaçada com `alt` "Conquista oculta".
  - [ ] 4.3 Selo de raridade (front-end spec 5.15; nome do componente fica a critério do @dev): ponto de cor de 8 px + nome da faixa + %; % no formato do idioma via `Intl.NumberFormat` (3,1 % em PT, 3.1% em EN); sem % mostra "sem dado" em `text-muted`, sem ponto. No celular a faixa pode abreviar ("M. rara"), a % nunca some.
  - [ ] 4.4 Lista em coluna única de até 960 px; `tabular-nums` nas %.
  - [ ] 4.5 Link "Pular para a lista de conquistas" como primeiro item focável da página.

- [ ] **Task 5: Spoiler sem vazamento** (AC: 4)
  - [ ] 5.1 Para conquista oculta, o servidor renderiza: `<button aria-expanded="false" aria-controls="ach-X-body">` com o texto acessível "Conquista oculta. Revelar spoiler: nome, descrição e passo a passo"; barras genéricas de tamanho fixo, embaçadas, com `aria-hidden="true"`; o conteúdo real dentro de `<template id="ach-X-tpl">`.
  - [ ] 5.2 `SpoilerGate` (Client Component): ao clicar, clona o template no lugar, marca `aria-expanded="true"`, move o foco para o nome revelado e anuncia "Spoiler revelado" numa região `aria-live="polite"`.
  - [ ] 5.3 % global e ícone continuam visíveis na oculta.
  - [ ] 5.4 `<noscript>` explicando que revelar spoilers precisa de JavaScript.
  - [ ] 5.5 Oculta revelada ganha a etiqueta "Oculta" e fica revelada até recarregar.
  - [ ] 5.6 Transição de 150 ms na opacidade; nada com `prefers-reduced-motion: reduce`. O texto real nunca passa pelo estado borrado.

- [ ] **Task 6: Interruptor global de spoilers** (AC: 5)
  - [ ] 6.1 `SpoilerToggle` no cabeçalho do site: `role="switch"`, rótulo visível "Spoilers" (no celular, ícone de olho com `aria-label`), alvo de toque de 44 px.
  - [ ] 6.2 Ligado revela todos os spoilers da página na hora, sem recarregar.
  - [ ] 6.3 Visitante: salvo em `localStorage['cmp:spoilers']`. A parte da conta (`PATCH /api/me/preferences`) é da Story 3.1.
  - [ ] 6.4 Estado seguro até ler a preferência: spoilers fechados; a troca só revela, nunca esconde depois de mostrar.

- [ ] **Task 7: Registro de visita** (AC: 7)
  - [ ] 7.1 `src/app/api/visit/[appid]/route.ts`: `POST`, sem auth, `appid` validado por zod; `UPDATE steam_game SET last_visited_at = now() WHERE appid = $1 AND (last_visited_at IS NULL OR last_visited_at < now() - interval '1 hour')`; responde 204.
  - [ ] 7.2 Conferir o cabeçalho `Origin` contra `NEXT_PUBLIC_SITE_URL` (regra de mutações da arquitetura 13.1).
  - [ ] 7.3 No cliente, `navigator.sendBeacon('/api/visit/{appid}')` sem cookies, depois da hidratação. Nunca atualizar `last_visited_at` na renderização.

- [ ] **Task 8: Mensagens e acessibilidade** (AC: 1 a 6)
  - [ ] 8.1 Todos os textos novos em `messages/pt-BR.json` e `messages/en.json` com as mesmas chaves; sem travessão.
  - [ ] 8.2 Um H1 (nome do jogo), H3 por conquista; `lang` do documento pelo idioma.

- [ ] **Task 9: Testes** (AC: 2, 4, 7)
  - [ ] 9.1 Unitários da Task 1 (ordenação com % nula no fim e desempate pelo schema, faixas, nomes).
  - [ ] 9.2 Teste de componente (ou renderização no servidor) que confirma: o nome e a descrição da oculta **não** estão fora do `<template>` antes de revelar; depois do clique, estão, com `aria-expanded="true"`.
  - [ ] 9.3 Integração da rota `/api/visit/[appid]`: primeira chamada escreve, segunda na mesma hora não escreve, `appid` inválido responde erro de validação.
  - [ ] 9.4 Integração da arquitetura 9.7: o `revalidatePath` do cron (Story 1.2) invalida as duas URLs públicas, `/pt/jogo/{appid}` e `/en/game/{appid}`.
  - [ ] 9.5 Manual: navegação por teclado da página nos dois idiomas (spoiler, interruptor, pular para a lista), zoom de 200% e largura de 320 px.

## Dev Notes

### Stories anteriores

- 1.1: `pathnames` com `/jogo/[appid]` → `/en/game/[appid]`, `proxy.ts`, tokens de cor (incluindo
  a escala de raridade), tipografia, cabeçalho e rodapé, CSP com `img-src` liberando
  `https://*.steamstatic.com` e `https://steamcdn-a.akamaihd.net`.
- 1.2: `ensureGame(appid)` em `src/server/steam/catalog.ts`, tabelas `steam_game`,
  `steam_achievement`, `steam_achievement_text`, orçamento em `src/server/budget.ts`, cron que
  lê `last_visited_at`.
- Sem Dev Agent Record das anteriores ainda.

### Arquivos desta story

```text
src/app/[locale]/jogo/[appid]/page.tsx
src/app/api/visit/[appid]/route.ts
src/components/                 # AchievementCard, SpoilerGate, SpoilerToggle, selo de raridade, aviso sem guia
src/domain/ordering.ts          # variante sem guia
src/domain/rarity.ts
src/domain/naming.ts            # parte Steam
messages/pt-BR.json, messages/en.json
tests/unit/…, tests/integration/…
```

[Source: docs/fullstack-architecture.md#4 Estrutura do código] [Source: docs/fullstack-architecture.md#11.3] (`rarity.ts`)

### Renderização da página do jogo

ISR: `generateStaticParams` com os jogos com guia × 2 idiomas, `dynamicParams = true`,
`revalidate = 86400`; o cron chama `revalidatePath`. 404 em cache para app sem conquistas ou
inexistente; erro da Steam mantém a última versão boa. Nada de `new Date()` ou aleatório no
HTML. Visita por `sendBeacon` sem cookies, escrita no máximo 1x/h por jogo. Metadados com
`generateMetadata`, Open Graph com a capa e `alternates.languages` (`pt-BR`, `en`). `appid`
validado por zod; fora do intervalo, 404. Com `CI_BUILD=1`, `generateStaticParams` devolve lista vazia.
[Source: docs/fullstack-architecture.md#8.1 Páginas e estratégia de renderização] [Source: docs/fullstack-architecture.md#14.5]

`POST /api/visit/[appid]`: sem auth, marca visita, 204.
[Source: docs/fullstack-architecture.md#8.2 Route Handlers]

### Componentes

- Server Components por padrão; Client Components só onde há interação (`SpoilerGate`,
  `SpoilerToggle`). [Source: docs/fullstack-architecture.md#12.2]
- Estado no cliente: enquanto nada chegou, estado seguro (spoilers fechados). Nesta story só
  `localStorage['cmp:spoilers']`; `GET /api/me` e progresso entram no Épico 3.
  [Source: docs/fullstack-architecture.md#12.3]
- Spoiler: arquitetura 12.4 itens 1 a 6 e front-end spec 5.2 (texto real só no `<template>`,
  barras genéricas `aria-hidden`, botão real, foco e `aria-live`, `<noscript>`).
  [Source: docs/fullstack-architecture.md#12.4 Spoilers sem vazamento] [Source: docs/front-end-spec.md#5.2 Spoiler embaçado]
- Cartão (estados "Sem guia", "Oculta embaçada", "Oculta revelada", "Foco"), selo de raridade,
  aviso sem guia, interruptor global: front-end spec 5.1, 5.15, 5.11 e 5.9.
- Imagens da Steam com `<img>` direto do CDN, sem `next/image` (limite de 5 mil transformações
  no Hobby). [Source: docs/fullstack-architecture.md#12.6 Imagens]
- LCP: a capa é o elemento de LCP, com tamanho reservado. [Source: docs/front-end-spec.md#11]
- Raridade nunca só por cor; texto sempre junto. [Source: docs/front-end-spec.md#1.3]

### Algoritmos

- Ordem sem guia: % decrescente (nulo como `-1`), depois `displayOrder`; 3 casas decimais;
  `removed_at` fora. [Source: docs/fullstack-architecture.md#11.1 Ordem por dificuldade]
- Faixas de raridade: mesma função para selo e `d_eff`. [Source: docs/fullstack-architecture.md#11.3] [Source: docs/prd.md#FR2]
- Nomes por idioma e "Conquista oculta" como descrição de oculta sem texto.
  [Source: docs/fullstack-architecture.md#11.4 Nomes por idioma]

### Segurança

Nomes e descrições da Steam sempre como texto (React escapa), nunca `dangerouslySetInnerHTML`.
Mutações conferem `Origin` contra `NEXT_PUBLIC_SITE_URL`.
[Source: docs/fullstack-architecture.md#13.1 Tabela de controles]

### Lacunas encontradas

- **G1. Primitivas acessíveis.** O front-end spec 5 sugere Radix UI e deixa para o @architect;
  a stack não lista biblioteca. `button` e `role="switch"` nativos atendem esta story; registrar
  a escolha.
- **G2. Página de erro com busca.** O front-end spec 3.1 pede que AppID inexistente ou sem
  conquistas mostre "página de erro tratada com a busca", mas a busca é a Story 1.4. Nesta
  story, o `not-found` fica sem o campo de busca; a 1.4 acrescenta.
- **G3. Texto do erro de orçamento.** "Tente mais tarde" (arquitetura 9.2) não tem texto
  definido no front-end spec. Criar a mensagem nos dois idiomas e registrar.
- **G4. Origem do `appdetails` na primeira visita** depende da escolha da Story 1.2 (lacuna G2
  de lá, `not_found`).
- **G5. `/api/visit` fora da WAF** (validação B12, aceito): não precisa de ação.
- **G6. Busca no cabeçalho** entra na 1.4; "Entrar com a Steam" continua oculto até o Épico 3.

### Testing

- Vitest em `tests/unit/` (funções puras, testes de tabela) e `tests/integration/` (rotas com
  Postgres do CI e fixtures). Nenhum teste chama a Steam. [Source: docs/fullstack-architecture.md#14.1 Pirâmide]
- Casos obrigatórios da arquitetura 14.3: "% nula vai para o fim". [Source: docs/fullstack-architecture.md#14.3]
- O E2E com axe do fluxo visitante é da Story 2.4; aqui a conferência do spoiler fora do DOM é
  por teste de componente e manual. [Source: docs/fullstack-architecture.md#14.4]
- Tela nova: navegação por teclado nos dois idiomas antes de fechar. [Source: docs/prd.md#4 Testes] [Source: docs/front-end-spec.md#7.3 Testes]

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-09-25 | 1.0 | Criação a partir do PRD 1.2, arquitetura 1.1 e front-end spec 1.1 | @sm (River) |

## Dev Agent Record

### Agent Model Used

_A preencher pelo @dev._

### Debug Log References

_A preencher pelo @dev._

### Completion Notes List

_A preencher pelo @dev._

### File List

_A preencher pelo @dev._

## QA Results

_A preencher pelo @qa._
